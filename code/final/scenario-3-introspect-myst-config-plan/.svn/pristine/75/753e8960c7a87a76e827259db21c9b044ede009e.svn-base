printer.config=${printer-config.config}
printer.validate=${printer-config.validate}
printer.file=${printer-config.file}
printer-config.level=DEV
printer-config.path=${printer-config.path}
printer-config.pattern.level=${printer-config.pattern.level}
printer-config.find-pattern=${printer-config.find-pattern}
printer-config.validation-1=${printer-config.validation-1}
printer-config.validation-2=${printer-config.validation-2}